# JWT Secret used for signing and verifying JSON Web Tokens
JWT_SECRET=my-secret-key

# Rate limit: max requests per 15-minute window. Set high in dev/test to avoid hitting the limit.
RATE_LIMIT_MAX=10000